// import { describe, it } from "jest"

// import { extractCollection } from "./content_page.js"


describe("extractCollection", () => {
    describe("given a string URL with no path", () => {
        it("returns an empty string", () => {
            throw 1
        });
    });
});
